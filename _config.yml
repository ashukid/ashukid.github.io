title: Ashutosh Chandra
email: irm2015001@iiita.ac.in
description: Searching a battle worth fighting for !
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://ashukid.github.io"
github_username:  ashukid


# Build settings
markdown: kramdown
plugins:
  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/

plugins:
  - jekyll-feed
  - jekyll-seo-tag